//other exporting module segundo

let pelo = 'divino';

let tener = 'tengo';

function frase(una, dos) {
    
    return una  + 'el'  +  'pelo'  + dos
};

export { pelo, tener, frase as funcion };

export default 28;